Preparing VMWare VMs fr Migration

Step by step:

1. Configure RDP on VMs

2. Registry Edit
    HKLM > Software > Microsoft > Windows > CurrentVersion > Policys > System
    Create new DWORD Value
    Name: LocalAccountTokenFilterPolicy
    Value: 1
    
3. Make sure date/time zone matches region of RG/Resources